import drawHeader from '../layouts/drawHeader';
import drawGarage from '../layouts/drawGarage';
import drawFooter from '../layouts/drawFooter';

const drawGaragePage = () => {
    drawHeader();

    drawGarage();

    drawFooter();
};

export default drawGaragePage;
