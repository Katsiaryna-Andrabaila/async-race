export const BASE_URL = 'http://localhost:3000';

export const generatedAmount = 100;

export const LIMITS_PER_PAGES: { readonly [x: string]: number } = {
    garageLimitPerPage: 7,
    winnersLimitPerPage: 10,
};

export const defaultColor = '#000000';
